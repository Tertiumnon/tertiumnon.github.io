#!/usr/bin/env bash
# Pre-commit hook: type checking before committing

set -e

echo "🔍 Running pre-commit checks..."

# Run linting
echo "  → Checking TypeScript..."
bun run lint:ts:check || exit 1

echo "✅ Pre-commit checks passed!"
