import { ProjectControlPanelService } from "./project-control-panel.service";

describe("ProjectControlPanelService", () => {
	it("should be created", () => {
		expect(ProjectControlPanelService).toBeTruthy();
	});
});
